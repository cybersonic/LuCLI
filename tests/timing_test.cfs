// Test script for timing functionality
writeOutput("Hello from CFML!" & chr(10));
writeOutput("This is a simple test script." & chr(10));

// Simulate some work
for (i = 1; i <= 5; i++) {
    writeOutput("Processing item " & i & chr(10));
    sleep(100); // Sleep for 100ms
}

writeOutput("Script execution completed!" & chr(10));
